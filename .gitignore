.wrangler/
.claude/
node_modules/
.DS_Store

# secrets — pushed to GitHub Actions secrets via `gh lockbox dotenv push .env`.
# never commit this file. see .env.example for the schema.
.env
.env.local
.env.*.local

# generated by `npm run build` — checked in for now (no CI yet),
# but listed here so contributors know what's derived.
# public/zazen/
